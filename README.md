# Trading Bot Platform PHP (Archived)

> **Пример консольной платформы для алгоритмической торговли на криптовалютных биржах**

⚠️ **Статус проекта: АРХИВНЫЙ** — Разработка остановлена. Код публикуется как справочный материал для разработчиков торговых ботов, демонстрирующий подходы к построению устойчивых систем автоматической торговли.

---

## ⚠️ Важное предупреждение

**Этот проект НЕ рекомендуется для production использования.**

Причины архивирования:
- Достигнут **архитектурный тупик** — дальнейшее развитие требует полного переписывания
- Платформа стала **избыточно сложной** для отладки и добавления функционала
- Накоплен **технический долг**, требующий фундаментального рефакторинга

### Для чего этот код?

Проект демонстрирует:
- ✅ Подходы к контролю рисков в автоматической торговле
- ✅ Паттерны работы с множественными биржевыми API
- ✅ Распределенную архитектуру с резервированием (master/rescuer)
- ✅ Управление состоянием через БД с репликацией
- ✅ Декларативные ограничители (`BoundedValue`) для безопасности

**Используйте код как reference, а не as-is решение.**

---

## 🏗️ Что это такое?

Частично распределенная консольная платформа для централизованного управления несколькими торговыми депозитами (или субаккаунтами) на централизованных-же криптобиржах. Развертывание предполагается исключительно в среде Linux, можно теоретически в контейнерах Docker. 

### Ключевые особенности

**Архитектура:**
- Консольное PHP приложение с циклом ~1 минута
- MariaDB с двухсторонней репликацией основной БД tradimg для хранения заявок, сигналов, конфигурации
- REST API для взаимодействия с биржами
- Простой REST API для получения внешних сигналов с вашего сервера
- Распределенность через параллельный запуск двух экземпляров, дающая относительную отказоустойчивость

**Функциональность:**
- **Плавное исполнение**: типично 5-10 минут на полное заполнение позиции
- **Оптимизация проскальзывания**: пошаговое исполнение с адаптацией
- **Market Making (тестовый)**: размещение заявок на нескольких уровнях
- **Сеточная торговля**: примитивная реализация с фиксированным шагом
- **Батчинг ордеров**: группировка связанных заявок для отслеживания

**Поддерживаемые биржи:**
- Binance
- Bitfinex  
- BitMEX
- Deribit

---

## 🏗️ Архитектура

### Принцип работы

```
┌─────────────────────────────────────────────────────────────┐
│  TradingCore::Run()  — главный цикл (~1 мин интервал)       │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ TradingCore::Update()  — синхронизация с биржей       │  │
│  │  ├─ Проверка статуса ордеров                          │  │
│  │  ├─ Обновление позиций                                │  │
│  │  ├─ Загрузка внешних сигналов (REST API)              │  │
│  │  └─ Запись в БД (MariaDB с репликацией)               │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ TradingCore::Trade()  — расчет и выставление заявок   │  │
│  │  ├─ CalcTargetPos() — расчет целевых позиций          │  │
│  │  ├─ ExtSigVerify() — валидация внешних сигналов       │  │
│  │  ├─ SetupLimits() — настройка ограничений             │  │
│  │  └─ NewOrder() — размещение на бирже                  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**⏱️ Хардкод цикла:** Интервал 1 минута **не настраивается**, так как влияет на логику. На практике с некоторыми биржами и большим числом позиций одной минуты недостаточно для полной синхронизации, тогда обычно следует пропуск цикла.

### Модульная структура (трейты)

Проект разбит на специализированные трейты:

```
TradingCore (ядро)
├── TradingLoop      # Торговый цикл и логика исполнения
├── TradeLogging     # Структурированное логирование
├── MarketMaker      # Маркет-мейкинг (экспериментальный)
└── Reporting        # Отчетность и статистика
```

### Ключевые компоненты

#### 1. **TradingCore** (`trading_core.php`)
Центральное ядро системы. Управляет жизненным циклом бота:
- Инициализация подключений к биржам
- Загрузка конфигурации из БД
- Координация работы всех модулей
- Обработка событий и ошибок

#### 2. **TradingLoop** (`trading_loop.php`)
Реализует основной торговый цикл через метод `Trade()`:
```php
1. ScanForTrade()    — получение целевых позиций из БД
2. CalcTargetPos()   — расчет требуемых изменений с учетом коэффициентов
3. SetupLimits()     — настройка BoundedValue ограничителей
4. ExtSigVerify()    — верификация и приоритизация внешних сигналов
5. Trade()           — размещение ордеров с контролем рисков
```

**Особенность:** Плавное исполнение за несколько циклов (5-10 минут), а не мгновенное. По сути бот двигает заявки все ближе к спреду, и только в режиме спешки внутри спреда пытается провести исполнение. На неликвиде это все никак неизбавляет от большого проскальзывания, при резких движениях рынка оно может даже возрастать.

#### 3. **TradingContext** (`trading_context.php`)
Контейнер состояния для каждой торговой пары с умными переменными:
```php
class TradingContext {
    private array $vars = []; // BoundedValue переменные
    
    // Основные параметры:
    // - curr_pos: текущая позиция
    // - target_pos: целевая позиция (с zero_cross защитой)
    // - bias: абсолютное смещение (unsigned)
    // - amount: объем ордера (с max_abs)
    // - cost_limit: лимит стоимости
}
```

#### 4. **BoundedValue** (`lib/smart_vars.php`)
Умные переменные с декларативными ограничениями:
```php
$ctx->get_var('target_pos')
    ->constrain('zero_cross', 0)      // Запрет пересечения нуля
    ->constrain('max_abs', $max_pos)  // Лимит позиции
    ->onChange(function($new, $old) {
        // Реакция на изменение
    });
```

**Поддерживаемые ограничители:**
- `max_abs` — максимальное абсолютное значение
- `max/min` — диапазон значений
- `multiple_of` — кратность (для lot_size)
- `zero_cross` — запрет смены знака
- `callback` — пользовательская проверка

#### 5. **OrdersBatch** (`orders_batch.php`)
Группировка связанных ордеров:
- Отслеживание прогресса исполнения
- Автоматическое управление lifecycle
- Связь с внешними сигналами
- Статистика исполнения

#### 6. **ExternalSignal** (`ext_signals.php`)
Обработка торговых сигналов:
- Расчет дельты позиции
- Управление приоритетами
- Балансировка заполнения
#### 7. **MarketMaker** (`market_maker.php`)
**⚠️ Экспериментальный модуль:**
- Размещение заявок на нескольких уровнях от спреда
- Примитивная сеточная торговля (Grid) с фиксированным шагом
- Работает с внешними сигналами класса `GridSignal`

---

## 🏛️ Иерархия классов

### Уровень 1: Приложение (TradingCore)

**`TradingCore`** — самый перегруженный функциональностью класс, по сути реализующий всё приложение через композицию трейтов.

```
┌─────────────────────────────────────────┐
│          TradingCore                    │  ← Базовый класс приложения
│  (композиция функциональности)          │
├─────────────────────────────────────────┤
│  use TradingLoop;      // Торговля      │
│  use TradeLogging;     // Логирование   │
│  use MarketMaker;      // ММ (опц.)     │
│  use Reporting;        // Отчеты        │
├─────────────────────────────────────────┤
│  + Run()           // Главный цикл      │
│  + Update()        // Синхронизация     │
│  + Trade()         // Торговая логика   │
│  + LoadConfig()    // Конфигурация      │
└─────────────────────────────────────────┘
           ▲
           │
    ┌──────┴───────┬──────────────┬──────────────┐
    │              │              │              │
┌───┴────┐    ┌────┴─────┐   ┌────┴─────┐   ┌────┴─────┐
│Binance │    │ Bitfinex │   │  BitMEX  │   │ Deribit  │
│  Bot   │    │   BOT    │   │    BOT   │   │   BOT    │
└────────┘    └──────────┘   └──────────┘   └──────────┘
     ↓              ↓            ↓             ↓
   impl_       impl_        impl_         impl_
 binance.php bitfinex.php bitmex.php  deribit.php
```

**Наследники `TradingCore`** (impl_*.php):
- Определяют **конкретные инстансы ботов** для каждой биржи
- Задают `impl_name` (например, `'binance'`, `'deribit'`)
- Создают и инициализируют объект **движка** для конкретной биржи
- **НЕ добавляют** особой функциональности — вся логика в `TradingCore`

```php
// Пример: impl_binance.php
class BinanceTrading extends TradingCore {
    
    public function __construct($mysqli) {
        parent::__construct($mysqli);
        
        $this->impl_name = 'binance';  // Идентификатор
        
        // Создание движка для Binance
        $this->trade_engine = new BinanceEngine($this);
    }
}
```

### Уровень 2: Движки (TradingEngine)

**`TradingEngine`** — базовый класс для абстракции работы с биржами.

```
┌─────────────────────────────────────────┐
│         TradingEngine                   │  ← Базовый интерфейс
│  (абстрактный класс)                    │
├─────────────────────────────────────────┤
│  # абстрактные методы:                  │
│  + LoadTickers()                        │
│  + LoadPositions()                      │
│  + LoadOrders()                         │
│  + NewOrder()                           │
│  + CancelOrder()                        │
│  + GetOrderStatus()                     │
└─────────────────────────────────────────┘
           ▲
           │
           │
┌──────────┴─────────────────────────────┐
│      RestAPIEngine                     │  ← REST API логика
│  (общие методы для REST бирж)          │
├────────────────────────────────────────┤
│  # RequestPublicAPI()  // HTTP запросы │
│  # MakeNonce()         // Уникальные ID│
│  # EncodeParams()      // Кодирование  │
│  # ProcessRateLimit()  // Rate limiting│
│  # SwitchAPIServer()   // Failover     │
│  + get_btc_price()     // BTC курс     │
└────────────────────────────────────────┘
           ▲
           │
    ┌──────┴────────┬───────────┬──────────┐
    │               │           │          │
┌───┴──────┐   ┌────┴─────┐  ┌──┴────┐   ┌─┴──────┐
│ Binance  │   │ Bitfinex │  │BitMEX │   │Deribit │
│ Engine   │   │  Engine  │  │Engine │   │Engine  │
└──────────┘   └──────────┘  └───────┘   └────────┘
```

**Финальные классы движков** (наследники `RestAPIEngine`):
- **Полное абстрагирование** логики конкретной биржи
- Реализация всех методов интерфейса `TradingEngine`
- Специфичная логика: аутентификация, подписи, форматы данных
- Обработка особенностей API (quanto contracts, funding rates и т.д.)

```php
// Пример: BinanceEngine extends RestAPIEngine
class BinanceEngine extends RestAPIEngine {
    
    // Реализация абстрактных методов
    public function LoadTickers(): bool {
        $data = $this->httpGet('/api/v3/ticker/24hr');
        // Парсинг и сохранение в $this->tickers
    }
    
    public function NewOrder($params): ?OrderInfo {
        // Binance-специфичная логика размещения ордера
        $signature = $this->signBinance($params);
        return $this->httpPost('/api/v3/order', $params);
    }
    
    // Специфичные методы Binance
    protected function signBinance($params): string {
        // HMAC-SHA256 подпись для Binance
    }
}
```

### Архитектурная цель

```
Приложение (TradingCore)
    ↓ зависит от
Движок (TradingEngine/RestAPIEngine)
    ↓ абстрагирует
Биржа (Binance/BitMEX/Deribit API)
```

**Принцип разделения:**
1. **TradingCore** — бизнес-логика торговли, единая для всех бирж
2. **RestAPIEngine** — общие паттерны REST API (HTTP, retry, rate limiting)
3. **BinanceEngine/BitMEXEngine/...** — специфика конкретной биржи

**Результат:** Добавление новой биржи = реализация одного класса движка, без изменений в `TradingCore`.


### Диаграмма потока данных

```
   bot_instance.php
         │
         ├─ создает: $bot = new BinanceTrading($db)
         │            └─ impl_name = 'binance'
         │            └─ trade_engine = new BinanceEngine()
         │
         ├─ запускает: $bot->Run()
         │                 │
         │                 ├─ Update()
         │                 │   └─ $this->trade_engine->LoadTickers()
         │                 │       └─ BinanceEngine::httpGet('/api/v3/ticker')
         │                 │
         │                 └─ Trade()
         │                     └─ $this->trade_engine->NewOrder()
         │                         └─ BinanceEngine::httpPost('/api/v3/order')
         │
         └─ MariaDB ← сохранение состояния
```

### Почему так сложно?

**Изначальная цель:** Универсальность + безопасность + отказоустойчивость

**Что получилось:**
- ✅ Добавление бирж без изменения ядра
- ✅ Единые контроли рисков для всех бирж
- ✅ Распределенность с репликацией БД
- ❌ Избыточная сложность при отладке
- ❌ Архитектурный тупик для масштабирования

---

## 🚀 Развертывание

### ⚠️ Важно

**Развертывание платформы — нетривиальный процесс**, требующий:
- Загрузка зависимых компонентов с других моих репозиториев
- Конфигурации MariaDB репликации
- Настройки хостов на системном уровне в /etc/hosts, чтобы не изменять код 
- Развертывание сервера внешних сигналов
- По сути требуется два локальных хоста, в идеале с независимым интернетом, и один удаленный для сервера сигналов и вспомогательных компонентов. 

**Документация по установке будет создана и выделена в отдельный `INSTALL.md`** только по активным запросам желающих изучить платформу.

### Минимальные требования

- **PHP**: 8.1+
- **MariaDB**: 10.5+ 
- **Расширения PHP**: 
  - `mysqli`, `curl`, `json`, `pcntl`
  - `gmp` (для BitMEX HMAC подписей)

```

### Конфигурация

#### 1. Реквизиты БД задаются в `lib/db_config.php`
#### 2. Секретная часть ключа API обычно делиться на три части, две сохраняются через приложение pass в отдельные файлы, а разделитель в конфигурацию (БД). Экземпляр бота может считывать из переменных окружения сам ключ и его секрет. Если запускать ботов через bot_manager.php, ключи будут передаваться автоматически, при условии разблокированной БД паролей. 

```bash
    # Отладочный запуск бота для конкретной биржи
    php bot_instance.php binance_bot

    # С логированием в файл
    php bot_instance.php binance_bot > logs/stdout.log 2> logs/stderr.log       

    # в продакшен режим, через менеджер - все боты будут работать до полуночи UTC, и автоматически перезапускаться. 
    ./bot_manager.php
```

Платформа использует БД trading для всех настроек, где каждому боту предназначена таблица с префиксом config__, а все таблицы перечислены в config__table_map. 

**Распределенность:** При параллельном запуске двух экземпляров один автоматически становится резервирующим (rescuer role), обеспечивая отказоустойчивость. Подразумевается две связанные репликацией локальные БД, через которые экземпляры могут оценивать активность друг друга, для чего используется таблица bot__redudancy.

Детали конфигурации — см. `INSTALL.md` (будет опубликован по запросу). 

bot_instance.php          # Entry point для запуска
├── trading_core.php          # Ядро системы
├── trading_loop.php          # Торговый цикл (трейт)
├── trading_engine.php        # Биржевой движок
├── trading_context.php       # Контекст торговли
├── orders_batch.php          # Управление батчами
├── ext_signals.php           # Внешние сигналы
├── market_maker.php          # Маркет-мейкинг
├── reporting.php             # Отчетность
├── trade_logging.php         # Логирование
│
├── impl_*.php                # Реализации для бирж
│   ├── impl_binance.php      # Binance API
│   ├── impl_bitfinex.php     # Bitfinex API
│   ├── impl_bitmex.php       # BitMEX API
│   └── impl_deribit.php      # Deribit API
│
├── lib/                      # Библиотеки
│   ├── smart_vars.php        # BoundedValue, ArrayTracer
│   ├── common.php            # Общие утилиты
│   ├── db_tools.php          # Работа с БД
│   └── ...
│
├── tests/                    # Тесты
├── web-ui/                   # Веб-интерфейс
├── sql/                      # SQL схемы
├── logs/                     # Логи
└── data/                     # Временные данные
```

---

## 🔧 Настройка и конфигурация

### Параметры риск-менеджмента

Настраиваются в `SetupLimits()` через `BoundedValue`:

```php
// Ограничение целевой позиции
$ctx->get_var('target_pos')
    ->constrain('zero_cross', 0)           // Не менять long↔short
    ->constrain('max_abs', $this->max_pos); // Макс. позиция

// Ограничение объема ордера
$ctx->get_var('amount')
    ->constrain('min', $ti->lot_size)       // Минимальный лот
    ->constrain('max_abs', fn() => $this->MaxAmount());

// Ограничение стоимости
$ctx->get_var('cost_limit')
    ->constrain('min', $ti->cost_limit)
    ->constrain('max', $config->max_order_cost);
```

### Константы (bot_globals.php)

```php
// Стоимости
define('REPORTABLE_ORDER_COST', 100);      // Уведомления для крупных
define('NON_LIQUID_ORDER_COST', 500);      // Лимит для неликвидных
define('ABNORMAL_HIGH_COST', 50000);       // Аномально крупные

// Таймауты
define('POSITION_HALF_DELAY', 36000);      // 10 часов устаревания позиции
define('ORDER_TIMEOUT', 600);              // 10 минут на исполнение

// Флаги ордеров
define('OFLAG_DIRECT', 0x01);              // Прямо из сигнала
define('OFLAG_MARKET_MAKER', 0x02);        // Ордер MM
```

---

## 🎮 Использование

### Работа с позициями

```php
// Установка целевой позиции вручную
$bot->target_pos[$pair_id] = [
    'value' => 0.5,           // BTC
    'ts' => date('Y-m-d H:i:s'),
    'symbol' => 'BTC/USDT',
    'src_account' => 1
];

## 🎮 Как это работает

### Основной цикл

Вся логика запускается через `TradingCore::Run()`:

```php
// bot_instance.php
require_once 'impl_binance.php';

$bot = new BinanceTrading($mysqli);
$bot->impl_name = 'binance_bot';
$bot->trade_enabled = true;

// Главный цикл (~1 минута)
$bot->Run();  // Вызывает регулярно Update() → Trade()   
```

### Расчет целевых позиций
Платформа работает с **сальдо-позициями** (сумма всех позиций сигналов + административное смещение):

### Внешние сигналы

Загружаются через REST API с сервера сигналов, логику можно смотрет в ext_signals.php, в частности SignalFeed::LoadSignals

## 📈 Контроль рисков

### Многоуровневая защита

1. **BoundedValue ограничители** — автоматическое применение при set()
2. **Проверки ликвидности** — лимиты для неликвидных пар
3. **Контроль pending** — блокировка при активных ордерах
4. **Split-trade** — разделение при смене long↔short
5. **Проверка стоимости** — min/max на каждом этапе
6. **Age контроль** — устаревшие позиции уменьшаются

### Пример настройки лимитов

```php
protected function SetupRiskLimits(TradingContext $ctx): void {
    $pair_id = $ctx->pair_id;
    $ti = $ctx->tinfo;
    
    // Стоимость ордера
    $ctx->get_var('cost')
        ->constrain('min', $ctx->min_cost)
        ->constrain('max', $this->getLiquidityLimit($pair_id))
        ->constrain('max', $this->getAssetClassLimit($pair_id));
    
   ...
}
```

---

## 🔌 Интеграция бирж

### Добавление новой биржи

## 🔧 Интеграция новой биржи

Каждая биржа реализуется в отдельном файле `impl_<exchange>.php`:

```php
<?php
    require_once 'rest_api_common.php';

    class NewExchangeTrading extends RestAPIEngine {
        ...
    }
```    
## 🧪 Тестирование

⚠️ **Тестовое покрытие минимальное** — одна из причин архивирования проекта.

**Для production использования потребовалось бы:**
- Unit тесты для каждого модуля
- Integration тесты с mock биржами
- End-to-end тесты торгового цикла
- Тесты отказоустойчивости (rescuer mode)x->get_var('bias')->set(0.5);

---

## 📝 Логирование

### Цветные логи

Система использует escape-коды для раскрашивания:

```php
$this->LogMsg("~C92#SUCCESS:~C00 Order filled %s", $order_id);
$this->LogError("~C91#ERROR:~C00 API call failed: %s", $error);
$this->LogMsg("~C93#DEBUG:~C00 Position: %f", $position);
```

**Коды цветов:**
- `~C00` — сброс
- `~C91` — красный (ошибки)
- `~C92` — зеленый (успех)
- `~C93` — желтый (debug)
- `~C94` — синий (info)
- `~C95` — магента (важное)

### Структура логов

```
logs.td/
├── binance_YYYY-MM-DD.log    # Основные логи
├── errors_YYYY-MM-DD.log     # Только ошибки
├── orders_YYYY-MM-DD.log     # История ордеров
└── positions_YYYY-MM-DD.log  # Изменения позиций
```

Журналы в случае активного трейдинга, или повторяющихся сбоев могут очень активно расти, вплоть до нескольких гигабайт в сутки. Поэтому реализована автоматическая компрессия оных, при обработке штатного завершения приложения. В любом случае нужно с запасом выделить дисковое пространство. 
---

## 🐛 Отладка

### Функционал управляемого журналирования

```php
// Принудительно включение verbose логов для торгового цикла
$ctx->verbosity = 3;  // 0=quiet, 1=normal, 2=verbose, 3=debug
Отладочная пара debug_pair задается в таблице конфигурации бота, и может изменяться в реальном времени

// Трассировка изменений переменных
$ctx->dump_changes('bias');  // Покажет все изменения bias
## 🔒 Контроль рисков и безопасность

### Многоуровневая защита в коде

Основная цель платформы — **повышенная безопасность и стабильность**:

1. **BoundedValue ограничители** — автоматическое применение лимитов при установке значений
2. **Zero-cross защита** — запрет переворота позиции long↔short за один ордер
3. **Проверки ликвидности** — снижение лимитов для неликвидных пар
4. **Контроль pending** — блокировка новых ордеров при активном исполнении
5. **Age контроль** — устаревшие целевые позиции автоматически уменьшаются
6. **Split-trade** — принудительное разделение при смене направления
7. **Батчинг** — отслеживание прогресса группы связанных ордеров



## 📊 Мониторинг и управление

### Web UI

Базовый веб-интерфейс расположен в `web-ui/`:
- Просмотр текущих позиций
- Активные ордера
- История исполнений
- Логи в реальном времени
- Управление конфигурацией, в частности задание административных смещений позиций

В моем случае развертывается на локальном apache2 с libapache2-mod-php8.3, можно использовать соответствующий Docker контейнер.

### Telegram Bot

Управление через Telegram-бот реализовано в `tele-bot/trade_ctrl_bot.php`.

**Архитектура:**
```
TradingCore → event_sender.php → trade_ctrl_bot.php → Telegram API
```

- `event_sender.php` — локальный буфер событий (может аккумулировать при недоступности сервера)
- `trade_ctrl_bot.php` — мини-сервер на хостинге, прокси к Telegram Bot API

**Доступные команды:**
- `bot-status` — статус торговых экземпляров
- `restart` — перезапуск бота
- `pos_coef <value>` — изменение коэффициента позиций
- `pos_offset <pair> <offset>` — административное смещение позиции
- `signal` — управление внешними сигналами

Команды защищены системой прав доступа по Telegram User ID.

**Автоматические отчёты в канал:**
- Каждые 10 минут: отчёт о крупных заявках и группах мелких ордеров
- Каждый час: текущие позиции (PNG таблицы через `lib/table_render.php`)
- Два раза в день: состояние счета с графиком Equity
- Критические события: сбои, перезапуски, ошибки нехватки средств

**Пример position report:**
```
┌─────────────────────────────────────────────┐
│  Positions Report  2025-12-31 14:00 UTC    │
├──────────┬──────────┬──────────┬───────────┤
│ Pair     │ Target   │ Current  │ Bias      │
├──────────┼──────────┼──────────┼───────────┤
│ BTC/USDT │  0.5000  │  0.4850  │ +0.0150   │
│ ETH/USDT │ -2.3000  │ -2.3120  │ -0.0120   │
│ SOL/USDT │  10.500  │  10.450  │ +0.050    │
├──────────┴──────────┴──────────┴───────────┤
│ Total equity: $127,450 (+2.3% daily)       │
└─────────────────────────────────────────────┘
```

**Пример equity chart (дважды в день):**
- График изменения баланса за последние 7 дней
- Отметки крупных сделок
- Просадка и максимальный баланс
- Процентное изменение

**Примечание:** Команды управления защищены авторизацией по Telegram User ID, сейчас требуется прописывать пользователей вручную в БД.

---

## 🎓 Что можно изучить из этого кода?

### Для разработчиков торговых ботов:

1. **Архитектурные паттерны**
   - Разделение на трейты (traits) для модульности
   - Работа с состоянием через БД
   - Распределенная архитектура с резервированием

2. **Контроль рисков**
   - Декларативные ограничители (`BoundedValue`)
   - Многоуровневая валидация перед размещением ордеров
   - Обработка граничных случаев (устаревшие позиции, split-trade)

3. **Работа с биржами**
   - Унификация различных API (Binance, BitMEX, Deribit)
   - Обработка rate limits
   - Retry логика и error handling

4. **Управление состоянием**
   - Синхронизация позиций и ордеров
   - Батчинг связанных заявок
   - Логирование изменений с трассировкой

### Чего НЕ стоит копировать:

- ❌ Очень длинный код функций/методов, буквально простыни. Такое рефакторить сложно, и даже с помощью ИИ расшифровать не всегда получается корректно
- ❌ Недостаточное тестовое покрытие
- ❌ Хардкод интервала цикла (1 минута), в основном упирается в медленное REST-API
- ❌ Синхронную архитектуру платформы - в клиент-серверном программировании это дает много издержек с таймаутами ожидаемых запросов. 
---

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

Вы можете использовать этот код в любых целях, но **на свой риск** и **без гарантий**.

---

## 📞 Вопросы и поддержка

**⚠️ Активной поддержки проекта НЕТ.**

- **Issues**: GitHub Issues (для обсуждения кода, не для help desk)
- **Discussions**: GitHub Discussions (для общих вопросов)
- **INSTALL.md**: Будет опубликован по запросу желающих развернуть платформу

**Pull Request'ы** с исправлениями или документацией приветствуются, но активной разработки не ведется.

--- 
```

## 🏁 Заключение 

Этот проект — пример того, **насколько сложными могут быть системы автоматической торговли**, когда необходимы:
- Высокая надежность и отказоустойчивость
- Контроль рисков на каждом уровне
- Поддержка множественных бирж и стратегий
- Распределенная архитектура

В то же время, это пример того, как **архитектура может зайти в тупик** под давлением убытками и системно несерьёзном контроле за финансовой стороной. 
Важным провалом является отсутствие документации хода разработки, журнала сбоев и баг-фиксов, даже непосредственно приватного репозитория проекта за 4 года не существовало. Если вы захотите реализовать свою платформу, просчитайте заранее пирамиду фокуса: контроль рисков, оценка сделок с ведением статистики P/L, эффективная отчетность, автоматика сворачивания торговли при пороговых убытках. Собственно я о рисках, особенно коварных при маржинальном трейдинге, начал задумывать лишь последний год, но в финансовом плане уже оказалось поздно. Целиком сосредоточившись на ядре и технических аспектах, я сильно упустил момент когда торговля внезапно давать убытки в 5-7% за день, а срезать риски быстро не захотел. Автоматизировано уменьшение торгового коэффициента было например в феврале 2025, через 4 года от создания первых исходников. Основа финансового успеха в трейдинге, это навык сбережения депозита, а не сверхвысокая доходность (и как следствие риск). По минимуму вы должны иметь финансовый стоп, после которого торговля останавливатся вся и до полного разбора ситуации, исправления и проведения тестов.

Используйте этот код как **учебный материал**, но не как готовое решение. Написать что-то подобное в режиме итеративной эволюции проекта, с помощью команды программистов вероятно будет стоить сотни тысяч долларов, т.е. многое можно оценить во избежание многомесячной эпопеи. Причем скорость движения к результату при живой торговле будет очень медленной, так что лучше реализовать поддержку демо-трейдинга и оценить нагрузочную способность вашего решения в разных состояниях рынка и при различных искусственных сбоях. 

---

*Проект архивирован в декабре 2025 года.*

## ⚖️ Disclaimer

**ИСПОЛЬЗУЙТЕ НА СВОЙ РИСК.**

- Софт предоставляется **"AS IS"** без каких-либо гарантий
- Авторы **не несут ответственности** за финансовые потери
- Торговля криптовалютами сопряжена с **высокими рисками**, а маржинальная с **запредельными рисками**. 
- Проект **не рекомендуется** для production использования
- **Нет поддержки** и обязательств по исправлению багов

Если вы решите использовать этот код — вы делаете это **на свой страх и риск**.

---

## 🤝 Вклад в проект

Приветствуются Pull Request'ы! 

### Приоритеты разработки

- [ ] Больше exchange адаптеров (Bybit, OKX, Kraken)
- [ ] Улучшение тестового покрытия
- [ ] Документация API методов
- [ ] Веб-интерфейс (dashboard)
- [ ] Бэктестинг модуль
- [ ] Telegram бот для уведомлений

### Стиль кода

- PSR-12 coding standard
- Типизация почти везде где возможно (PHP 8.1+)
- Комментарии на русском/английском
- Ничтожный % вайб-кода, проект начался слишком давно )
- PHPDoc для всех public методов

---

## ⚠️ Disclaimer

**Используйте на свой риск!**

Этот софт предоставляется "как есть" без каких-либо гарантий. Авторы не несут ответственности за финансовые потери, возникшие в результате использования данного ПО.

**Торговля криптовалютами сопряжена с высокими рисками.** Тестируйте на демо-аккаунтах перед использованием реальных средств.

---

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

---

## 📞 Контакты

- **Issues**: GitHub Issues
- **Discussions**: GitHub Discussions
- **Email**: [указать email если публичный]

---

## 🙏 Благодарности

- Сообществу PHP за отличный язык
- Биржам за API документацию
- Всем контрибьюторам проекта

---

Данный файл составлен с помощью Copilot AI (Claude Sonnet 4.5) и может содержать неточности, даже фантазии. 