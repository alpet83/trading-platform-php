; Runtime PHP logging for container integrity checks
log_errors=On
error_reporting=E_ALL
display_errors=Off
html_errors=Off
; Must be provided by each service in docker-compose for per-container logs.
error_log=${PHP_ERROR_LOG_FILE}
